#! /bin/bash

# this is used in startup.sh because we want to open a terminal and execute the htop command
# we couldn't pass the command `htop` but we can pass a shell script to execute the command
htop



